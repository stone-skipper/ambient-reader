export default function Contact({ darkMode }) {
  return (
    <div
      style={{
        position: "fixed",
        bottom: 20,
        right: 20,
        textAlign: "right",
        color: darkMode === true ? "white" : "black",
        opacity: 0.7,
      }}
    >
      {/* Created by Seungmee Lee */}
    </div>
  );
}
